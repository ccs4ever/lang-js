{
  "name": "@cuelang/cue",
  "version": "0.0.14",
  "description": "An npm package for using cuelang from js",
  "scripts": {
    "build": "yarn build:go",
    "build:go": "gopherjs build -o ./build/cue.js ./src/cue.go",
    "test": "NODE_ENV=test tape '{,!(node_modules)/**/}*.test.js'"
  },
  "devDependencies": {
    "tape": "^4.11.0"
  }
}
